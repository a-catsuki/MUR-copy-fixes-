if (Initialisation.ReadyToDrive eq ReadyToDrive.Ready){
	RHS.SetState(true);
	LHS.SetState(true);
	}
// testing only
else if (Test Fan High eq 1){
	RHS.SetState(true);
	LHS.SetState(true);
	}
else{
	RHS.SetState(false);
	LHS.SetState(false);
	}